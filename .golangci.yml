version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
issues:
  exclude-rules:
    # Test files get more latitude
    - path: "_test\\.go"
      linters:
        - errcheck
